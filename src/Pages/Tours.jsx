import React from 'react';
import data from '../data';

const Tours = () => {
 let thing = data
 console.log(thing)
    return (
        <div>
        
        </div>
      )
};

export default Tours;